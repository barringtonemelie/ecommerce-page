import * as React from 'react';
import { Button, Counter } from './index.jsx';

export default function ProductPage() {
    return (
        <section>
            <Counter />
            <Button />
        </section>
    )
}